using UnityEngine;
using UnityEngine.Events;

namespace aaaa
{
    public class aaaa : MonoBehaviour
    {
        public static UnityEvent<int> a;
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }

}
